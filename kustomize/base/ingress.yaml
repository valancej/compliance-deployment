apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: example-webapp
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              serviceName: example-webapp
              servicePort: http